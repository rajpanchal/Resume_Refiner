<% include ../partials/header %>
  <header class="jumbotron">
    <h1><span class="glyphicon glyphicon-tent align-bottom" aria-hidden="true"></span> Welcome to YelpResume!</h1>
    <p>View our hand-picked ResumeRefiners around the world</p>
    <form class="form-inline" action="/ResumeRefiners" method="GET">
      <div class="form-group has-feedback">
        <label for="search"></label>
        <input class="form-control" type="text" name="search" placeholder="Search ResumeRefiners" autocomplete="off" />
        <span class="glyphicon glyphicon-search form-control-feedback" aria-hidden="true"></span>
      </div>
    </form>
    <hr>
    <a class="pull-right" href="/ResumeRefiners/new">
      <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
      Share New ResumeRefiner
    </a>
  </header>

  <div class="row text-center" style="display: flex; flex-wrap: wrap;">
    <% if (noMatch !== null) {%>
      <div class="col-md-12">
        <h4><%= noMatch %></h4>
      </div>
    <% } %>
    <% ResumeRefiners.forEach(ResumeRefiner => {%>
      <div class="col-md-3 col-sm-6">
        <div class="thumbnail card">
          <img class="ResumeRefiner__image" data-src="<%= ResumeRefiner.image.url %>" alt="<%= ResumeRefiner.name %>" />
          <div class="caption">
            <h4 class="caption"><%= ResumeRefiner.name %></h4>
          </div>
          <p>
            <a class="btn btn-primary btn-sm btn-b-mg" href="/ResumeRefiners/<%= ResumeRefiner._id %>">
              More Info
            </a>
          </p>
        </div>
      </div>
    <% })%>
  </div>
<% include ../partials/footer %>
